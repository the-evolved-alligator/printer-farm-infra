# Agent instructions

1. **Start here**: read `AGENT_HANDOFF.md` for host layout, Git vs live Docker paths, Klipper/USB notes, and what is already done.
2. **Then**: `VISION.MD` (roadmap) and `INFRASTRUCTURE.MD` (operational truth, including **Live host paths**).
3. **Layout**: see `docs/DIRECTORY_LAYOUT.md`.

When changing infrastructure: update `INFRASTRUCTURE.MD` and, if applicable, both the live Compose file under `/home/daniel-quicazan/docker/` and the repo copy under `docker/` in this repository, then commit.
